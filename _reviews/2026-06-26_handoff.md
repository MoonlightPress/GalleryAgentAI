# HANDOFF — Mochi's Atelier (resume here after context clear)

**For the next session:** read this + `CURRENT_STATE.md` + `_reviews/2026-06-25_saffron_wholesite_review.md` first. This is GEGYjiji's PRIVATE dashboard (a Tokyo watercolor artist, the woman Scott built it for). Project: `C:\ScottStuff\GalleryAgentAI` (own git repo, `origin/main`). Deploy = Scott runs `! cd /c/ScottStuff/GalleryAgentAI && bash deploy.sh` (the agent can't SSH; deploy force-pushes the profile + regenerates the career report; tell Scott to hard-refresh after). NEVER `git add .` (tracked `.env` has secrets) — stage explicit paths.

## State (live now)
Latest deployed commit ~`ae136f68`. All green (frontend 39/39, python 199, build, lint 0 err). Live: corrected real exhibition record (12 shows, Level 3 not the old overshoot), de-pressured money page, honest cards (no fabricated taste-fit), white-balanced **watercolor icon set** replacing emoji, Landscape tab cut, leaner Profile, links everywhere, weekly cadence + shy tips, Peppercorn white-screen crash fixed.

## Standing constraints (always)
- No store/sales references and no age/age-countdowns in her-facing copy. Instagram ~27k (don't hard-code brittle numbers; "established, growing following").
- Money-section tone is the gold standard: calm, optional, affirming, never rank/quota/pressure. Match it everywhere.
- The open-questions belong in **Peppercorn**, not Profile (already removed from Profile).
- Both viewports (phone ~390px + laptop ~1024–1440). 
- **Use the `frontend-design` skill** for all visual/design work (Scott explicitly asked; not using it = why the "banner" kept collapsing to a thin line).
- The email-draft regen (compact_opportunities.json `email_en` + reports/inquiry_drafts/*.txt) keeps re-appearing as an engine side-effect — has been reverted each commit; it's a separate review-before-ship item, don't bundle it.

## Watercolor icon set (assets + wiring)
- Assets: `frontend/public/icons/ic_*.webp` (128px). 13 done + the pawprint below. All **white-balanced to pure white** (script: `scratchpad/whiten_icons.js`, abs-require sharp at `frontend/node_modules/sharp`) so `mix-blend-mode:multiply` (in the CSS) erases the bg on the white cards. Any NEW icon must be white-balanced the same way or it'll show a square.
- Wiring: `OppCard.jsx` `CAT_ICON` (per-card category → icon), `OpportunitiesSection.jsx` `SECTION_ICONS` (section headers), and `RelationshipTargets.jsx` (People — still has its own emoji `TYPE_ICON`, see below). Helper renders `ic_*` values as `<img>` with multiply.

## PENDING WORK (prioritized) — Scott's open notes
1. **SECTION-HEADER BANNERS (the big one, repeated 3×).** Current header = small icon + title + a thin orange line; Scott wants a real BANNER — "something beautiful with light framing accents," bigger, and **distinct from the per-card icons**. Design it via the frontend-design skill (signature element; quiet everything else). Direction floated: a hand-labeled sketchbook-plate feel — soft wash band + hairline sepia/gold frame with small corner accents + title in the display serif (IM Fell English) larger + the icon larger inside it. Files: `OpportunitiesSection.{jsx,css}` + the Saffron section headers (`SaffronPage.jsx` `SectionShell`/`sf-section-title`).
2. **Unify People (`RelationshipTargets`)** — it's a *separate component* (own `.rt-*` CSS, own card markup, own **emoji** `TYPE_ICON` at lines ~32–38) so it keeps drifting from the rest. Replace the emoji with the watercolor icons and share the card/section styling (or render through the shared components) so it stops being a snowflake. Apply the same banner header.
3. **Mouse → pawprint** for "strongest picks / immediate_best_moves." The real-mouse read as off-brand Peppercorn (Peppercorn = the mouse persona); Mochi's a cat → her PAW on her best finds. Pawprint generated this session: fal URL `https://v3b.fal.media/files/b/0a9fc2b4/H5q9F3NTKugC4Cdl_2Bdi.jpg` (download + white-balance + save as `public/icons/ic_pawprint.webp`; or regenerate — prompt: "single cat paw print (one central pad + four toe beans), soft loose watercolor, warm grey/dusty rose/terracotta/amber/ink, pure white bg, no text"). Then set `SECTION_ICONS.immediate_best_moves` and the Strongest-Picks "✦" header from `ic_mouse` → `ic_pawprint`.
4. **Press/Visibility header** — the folded-newspaper icon is weak; regenerate a better one (open magazine, or a spotlight for "visibility").
5. **Bigger icons** — card icons up again (currently 52px), header icons bigger inside the banner.
6. **Spacing** — balance the gap under "show more" vs sections with no show-more, so section separation is even either way.
7. **Icon taxonomy** — prizes (e.g. Aesthetica Art Prize) show the envelope because they're categorized as an open-call; the ribbon (`ic_award`) is competitions. Fix by re-categorizing prize-type opportunities (data: `deploy_data/compact_opportunities.json`) so prizes map to the ribbon — not by fudging the icon.

## SHIPPED 2026-06-26 (Saffron + Peppercorn pass) — committed, NOT yet deployed
Branch: `main`. Commits `26634c65`, `663a868b`, `6e2eb26f`, `0aa70811`, `3aee993e`, `5857c64c`.
All green: frontend 39/39, python 199/199, lint 0 err (1 pre-existing PeppercornPage warning), build OK, api.py compiles.
**Deploy:** Scott runs `! cd /c/ScottStuff/GalleryAgentAI && bash deploy.sh`, then hard-refresh. **Review live in zh** (her default).

Saffron (Scott's punch-list, all done):
- White container box behind everything → `.sf-content` transparent; cards sit on parchment.
- Career-unlocks vertical connector line → removed.
- Career Position → markers restyled (accent rule, not stray dots) + lead spacing + a real "where you stand" synopsis; number-only summaries (Career Position / Benchmarks / Momentum / Publication) → readable synopses.
- "You're in Good Company" → 13 peers (her 4 good comps + 9 newly researched, verified, daily-diary-weighted; verified IG links; zh wired).
- Calendar redesigned (ink dot under day, not grey blocks + pink pills); Timing Intelligence restyled + a real reading.
- Press & Pitch outlets linked; Revenue links only when a real platform (else plain text).
- Collaboration Map rebuilt (real named/linked targets in 3 groups + how-to); Collector Ecosystem rebuilt (real channels + how-to); Press Kit added (how-to + ready-to-use sample, bottom of press section); Licensing subheader → short overview, detail moved into a card lead.
- Venue Tracker now savable (status/date/note → PATCH /api/contacts/{name}; api payload carries notes).

Peppercorn (Scott, same day):
- Section order made deterministic: statement → preferences → goals → questions → (then the long logs). The old score-sort buried preferences below all four logs.
- Sub-navigation (filter chips) added to the submission / exhibition / venue logs (contacts already had it).

**Known follow-up gap:** collaboration + collector prose is zh + en; **ja falls back to English** for those two (press kit is full zh/ja/en). Fill via the translate path when convenient. Also: the old `sf.sec.collab/collector/...` translation keys + the api `collaboration_map`/`collector_ecosystem` payload are now unused (harmless; can be pruned later). New comparable-artist peers leak English in **ja** only.

## DONE — earlier Saffron notes (superseded by the SHIPPED block above)
- a. white box ✓  b. vertical line ✓  c. number summaries ✓  d. 4 peers → 13 ✓

## PENDING — bigger / deliberate (don't half-build)
- **Saffron intro retune** — drop the "who's showing where / which artists are rising" promise (Landscape was cut). Proposed: *"Hi, I'm Saffron. I keep an eye on the bigger picture of your work — where you stand, what you've built, and the doors worth walking toward next. Come up and I'll show you the view."* Awaiting Scott's final wording OK; then apply (`SaffronPage.jsx` SaffronIntro) + redeploy.
- **Peer-venue research feature** ("where her watercolor peers show in Tokyo") — genuinely valuable and is what Landscape *should* have been, but needs a real research pass (gather where her named comparable artists exhibit). Scope as its own project; do not half-build.

## How to resume
Tell the fresh chat: "continue the Mochi handoff" → read this doc, pick up the PENDING work (banners first, via the frontend-design skill), commit per chunk, hand Scott the deploy. Verify the live exhibition list is showing all 12 (it's a cache thing on Scott's end — hard refresh).
