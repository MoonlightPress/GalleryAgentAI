deploy_package/
.env
.env.*
*.env
.claude/worktrees/
**/__pycache__/
*.pyc

# Secrets / keys — never commit
*.pem
Web/*.pem

# Logs
*.log
logs/

# Local junk
temp/

# local backups / sandbox reset points
_prewipe_backup_*/
_sandbox_reset_point/
# timestamped per-edit snapshots of the artist's data (runtime, never committed)
memory/backups/

# stray file created when a bash-run script redirects to Windows NUL
NUL
